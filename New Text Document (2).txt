# 🚀 دليل البداية السريعة - DEVIX Store

دليل مختصر لتشغيل المتجر في أقل من 10 دقائق!

---

## 📦 الطريقة 1: تشغيل Frontend فقط (الأسرع)

### الخطوات:

1. **حمّل الملفات:**
```bash
# افتح مجلد المشروع
cd devix-store
```

2. **افتح في المتصفح:**
- افتح ملف `index.html` مباشرة في المتصفح
- أو استخدم Live Server في VS Code

3. **جرّب الموقع:**
- تصفح المنتجات ✅
- أضف للسلة ✅
- سجل حساب (تجريبي) ✅
- جرب الدفع (وضع demo) ✅

**⚠️ ملاحظة:** في هذا الوضع، الـ Discord webhook والإيميلات لن تعمل.

---

## 🔧 الطريقة 2: تشغيل مع Backend (كامل المزايا)

### المتطلبات:
- Node.js 16 أو أحدث
- npm أو yarn

### الخطوات:

#### 1. تثبيت التبعيات:
```bash
npm install
```

#### 2. إنشاء ملف .env:
```bash
# انسخ ملف المتغيرات
cp .env.example .env

# افتح .env وأضف مفاتيحك
nano .env
```

#### 3. أضف Discord Webhook:
```bash
# في ملف .env
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_HERE
```

كيف تحصل على Webhook:
1. اذهب لـ Discord Server
2. Server Settings → Integrations → Webhooks
3. New Webhook
4. انسخ URL

#### 4. شغّل السيرفر:
```bash
npm run dev
```

#### 5. افتح المتصفح:
```
http://localhost:3000
```

**🎉 تمام! الموقع يعمل الآن بكامل المزايا**

---

## 🎮 إعداد Discord (مهم)

### لماذا Discord مطلوب؟
- ✅ إشعارات فورية بالطلبات
- ✅ معلومات العملاء الجدد
- ✅ تنبيهات الدفع
- ✅ رسائل الدعم الفني

### خطوات الإعداد:

1. **إنشاء Discord Server (إذا لم يكن لديك):**
   - افتح Discord
   - اضغط + → Create Server
   - سمّه "DEVIX Store"

2. **إنشاء قنوات منفصلة:**
```
📁 DEVIX Store
  ├── 📢 announcements
  ├── 🛒 orders
  ├── 💳 payments
  ├── 👥 new-users
  └── 💬 support
```

3. **إنشاء Webhooks لكل قناة:**
```bash
# مثال:
Orders Channel → Settings → Integrations → Webhooks
Name: Orders Bot
Channel: #orders
Copy URL
```

4. **تحديث الكود:**
```javascript
// في script.js أو server.js
const WEBHOOKS = {
    orders: 'webhook_url_1',
    payments: 'webhook_url_2',
    users: 'webhook_url_3',
    support: 'webhook_url_4'
};
```

---

## 📧 إعداد البريد الإلكتروني

### الخيار الأسهل - EmailJS:

1. **التسجيل:**
   - اذهب إلى [emailjs.com](https://emailjs.com)
   - سجل حساب مجاني

2. **ربط Gmail:**
   - Add Email Service
   - اختر Gmail
   - اتبع الخطوات

3. **إنشاء Template:**
```
Subject: تأكيد طلبك #{{order_number}}

مرحباً {{customer_name}}،
تم استلام طلبك بنجاح!

المنتجات: {{products}}
المجموع: {{total}} ريال

شكراً لك!
```

4. **أضف في HTML:**
```html
<script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
<script>
  emailjs.init("YOUR_PUBLIC_KEY");
</script>
```

---

## 💳 إعداد بوابة الدفع (اختياري)

### للإنتاج - استخدم Stripe:

1. **التسجيل:**
```bash
# اذهب إلى stripe.com
# سجل حساب
```

2. **احصل على المفاتيح:**
```
Dashboard → Developers → API Keys
- Publishable key: pk_test_...
- Secret key: sk_test_...
```

3. **أضف في .env:**
```bash
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
```

4. **تثبيت Stripe:**
```bash
npm install stripe
```

### للاختبار - الوضع الحالي:
- يعمل بدون بوابة حقيقية
- استخدم أي رقم بطاقة للتجربة

---

## ✅ اختبار التكامل الكامل

### 1. اختبر Discord Webhook:
```bash
# في Console المتصفح (F12):
fetch('YOUR_WEBHOOK_URL', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    content: 'اختبار من DEVIX Store ✅'
  })
});
```

### 2. اختبر تسجيل مستخدم:
1. افتح الموقع
2. اضغط "تسجيل الدخول"
3. املأ النموذج
4. اربط Discord (تجريبي)
5. أنشئ الحساب
6. تحقق من Discord → يجب أن يصل إشعار

### 3. اختبر طلب كامل:
1. أضف منتجات للسلة
2. اذهب للسلة
3. إتمام الطلب
4. أدخل معلومات الدفع
5. اضغط "دفع آمن"
6. تحقق من:
   - ✅ Discord: إشعار الطلب
   - ✅ Email: رسالة تأكيد
   - ✅ الموقع: رقم الطلب

---

## 🐛 حل المشاكل السريع

### المشكلة: Discord لا يستقبل
**الحل:**
```javascript
// تأكد من:
1. Webhook URL صحيح
2. لا يوجد مسافات زائدة
3. الرابط يبدأ بـ https://discord.com/api/webhooks/

// اختبر مباشرة:
curl -X POST "YOUR_WEBHOOK_URL" \
  -H "Content-Type: application/json" \
  -d '{"content": "test"}'
```

### المشكلة: Emails لا ترسل
**الحل:**
```javascript
// تأكد من:
1. EmailJS account مفعّل
2. Email Service مربوط
3. Template موجود
4. Public Key صحيح

// اختبر في Console:
emailjs.send('service_id', 'template_id', {
  to_email: 'test@example.com',
  message: 'test'
});
```

### المشكلة: Port 3000 مستخدم
**الحل:**
```bash
# غيّر البورت
PORT=3001 npm run dev

# أو أوقف العملية على 3000
lsof -ti:3000 | xargs kill
```

---

## 📱 نشر الموقع (Deploy)

### Netlify (الأسهل):
```bash
# 1. ارفع الملفات على GitHub
git init
git add .
git commit -m "Initial commit"
git push

# 2. اذهب إلى netlify.com
# 3. New site from Git
# 4. اختر repository
# 5. Deploy!
```

### Vercel:
```bash
npm install -g vercel
vercel login
vercel
```

### Railway (للـ Backend):
```bash
# 1. اذهب إلى railway.app
# 2. New Project → Deploy from GitHub
# 3. أضف المتغيرات البيئية
# 4. Deploy
```

---

## 🎯 الخطوات التالية

بعد التشغيل بنجاح:

1. ✅ **خصص التصميم:**
   - غيّر الألوان في `style.css`
   - أضف لوجو خاص بك
   - عدّل النصوص

2. ✅ **أضف منتجات:**
   - عدّل مصفوفة `products` في `script.js`
   - أضف صور حقيقية

3. ✅ **فعّل بوابة دفع حقيقية:**
   - سجل في Stripe/PayTabs
   - اتبع دليل التكامل

4. ✅ **أضف قاعدة بيانات:**
   - MongoDB للمرونة
   - MySQL للاستقرار

5. ✅ **حسّن الأمان:**
   - أضف HTTPS
   - استخدم JWT
   - فعّل Rate Limiting

---

## 📞 الدعم

**مشاكل؟ أسئلة؟**

1. راجع [README.md](README.md) الكامل
2. تحقق من Console (F12) للأخطاء
3. تأكد من المتغيرات البيئية
4. جرب على متصفح آخر

---

## 🎉 مبروك!

موقعك جاهز الآن! 🚀

ابدأ البيع وحظاً موفقاً 💪

---

**DEVIX Store © 2024**