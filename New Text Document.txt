# 🚀 DEVIX Store - دليل التثبيت والإعداد الكامل

متجر إلكتروني احترافي متكامل مع نظام دفع آمن وربط Discord وإشعارات بريد إلكتروني

---

## 📋 المحتويات

1. [الملفات المطلوبة](#الملفات-المطلوبة)
2. [التثبيت السريع](#التثبيت-السريع)
3. [إعداد Discord Webhook](#إعداد-discord-webhook)
4. [إعداد بوابة الدفع](#إعداد-بوابة-الدفع)
5. [إعداد خدمة البريد الإلكتروني](#إعداد-خدمة-البريد-الإلكتروني)
6. [الميزات الرئيسية](#الميزات-الرئيسية)
7. [استخدام الموقع](#استخدام-الموقع)
8. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 📁 الملفات المطلوبة

هيكل المشروع:

```
devix-store/
├── index.html              # الصفحة الرئيسية
├── style.css               # ملف التنسيقات
├── script.js               # البرمجة الرئيسية
├── webhook-handler.js      # معالج Discord Webhook
└── README.md              # هذا الملف
```

---

## ⚡ التثبيت السريع

### 1. تحميل الملفات

قم بتحميل جميع الملفات في مجلد واحد على استضافتك أو على جهازك المحلي.

### 2. الاستخدام المحلي (للتجربة)

```bash
# افتح ملف index.html مباشرة في المتصفح
# أو استخدم Live Server في VS Code
```

### 3. رفع على الاستضافة

#### استضافة مجانية موصى بها:
- **Netlify**: [netlify.com](https://netlify.com)
- **Vercel**: [vercel.com](https://vercel.com)
- **GitHub Pages**: [pages.github.com](https://pages.github.com)

#### خطوات الرفع على Netlify:
1. اذهب إلى [netlify.com](https://netlify.com)
2. اسحب المجلد بالكامل إلى Netlify
3. انتظر النشر التلقائي
4. سيعطيك رابط مباشر للموقع

---

## 🎮 إعداد Discord Webhook

### خطوة 1: إنشاء Webhook

1. افتح Discord واذهب إلى السيرفر الخاص بك
2. اضغط على إعدادات السيرفر (⚙️)
3. اذهب إلى **Integrations** → **Webhooks**
4. اضغط **New Webhook**
5. سمّ الـ webhook "DEVIX Orders" أو أي اسم تريده
6. اختر القناة التي تريد الإشعارات فيها
7. انسخ رابط الـ Webhook URL

### خطوة 2: إضافة الرابط للكود

افتح ملف `script.js` وابحث عن:

```javascript
const CONFIG = {
    DISCORD_WEBHOOK: 'YOUR_DISCORD_WEBHOOK_URL_HERE',
    ...
};
```

واستبدل `YOUR_DISCORD_WEBHOOK_URL_HERE` برابط الـ webhook الخاص بك:

```javascript
const CONFIG = {
    DISCORD_WEBHOOK: 'https://discord.com/api/webhooks/123456789/abcdefghijklmnop',
    ...
};
```

### خطوة 3: اختبار الـ Webhook

```javascript
// اختبر الـ webhook في Console المتصفح:
const webhook = new DiscordWebhook('رابط_الwebhook_هنا');
await webhook.send({
    title: 'اختبار',
    description: 'تجربة الإشعار',
    color: 3447003
});
```

---

## 💳 إعداد بوابة الدفع

### خيار 1: بوابة الدفع الحقيقية (للإنتاج)

#### استخدام Stripe (موصى به عالمياً):

1. **إنشاء حساب Stripe**
   - اذهب إلى [stripe.com](https://stripe.com)
   - سجل حساب جديد
   - احصل على API Keys من [Dashboard → Developers → API Keys](https://dashboard.stripe.com/apikeys)

2. **تثبيت Stripe في المشروع**
   
   أضف في `<head>` في index.html:
   ```html
   <script src="https://js.stripe.com/v3/"></script>
   ```

3. **تحديث كود الدفع**
   
   في `script.js`:
   ```javascript
   const stripe = Stripe('pk_test_YOUR_PUBLISHABLE_KEY');
   
   async function handlePayment(e) {
       e.preventDefault();
       
       // إنشاء Payment Intent
       const response = await fetch('/create-payment-intent', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ 
               amount: total * 100, // بالهللات
               currency: 'sar'
           })
       });
       
       const { clientSecret } = await response.json();
       
       // تأكيد الدفع
       const result = await stripe.confirmCardPayment(clientSecret, {
           payment_method: {
               card: elements.getElement(CardElement),
               billing_details: {
                   name: cardName
               }
           }
       });
       
       if (result.error) {
           showToast('❌ فشل الدفع: ' + result.error.message, 'error');
       } else {
           // الدفع نجح
           await processSuccessfulPayment(result.paymentIntent);
       }
   }
   ```

#### استخدام PayTabs (للسعودية):

1. سجل في [paytabs.com](https://paytabs.com)
2. احصل على API credentials
3. اتبع [دليل التكامل](https://site.paytabs.com/en/paytabs-api-documentation/)

#### استخدام Moyasar (للسعودية):

1. سجل في [moyasar.com](https://moyasar.com)
2. احصل على API Key
3. راجع [التوثيق](https://moyasar.com/docs/api/)

### خيار 2: الاختبار (Demo Mode)

الكود الحالي في وضع Demo للاختبار. يمكنك استخدام:
- أي رقم بطاقة (مثل: 4242 4242 4242 4242)
- أي تاريخ انتهاء مستقبلي
- أي CVV

---

## 📧 إعداد خدمة البريد الإلكتروني

### خيار 1: EmailJS (الأسهل - مجاني)

1. **إنشاء حساب**
   - اذهب إلى [emailjs.com](https://emailjs.com)
   - سجل حساب مجاني

2. **إعداد الخدمة**
   - اذهب إلى **Email Services** → **Add New Service**
   - اختر Gmail أو أي خدمة بريد
   - اربط حسابك

3. **إنشاء Template**
   - اذهب إلى **Email Templates** → **Create New Template**
   - صمم قالب البريد:
   ```
   مرحباً {{to_name}}،
   
   شكراً لطلبك #{{order_number}}
   
   المنتجات:
   {{products}}
   
   المجموع: {{total}} ريال
   ```

4. **التكامل مع الموقع**
   
   أضف في `<head>`:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
   ```
   
   في `script.js`:
   ```javascript
   emailjs.init("YOUR_PUBLIC_KEY");
   
   async function sendEmail(data) {
       try {
           await emailjs.send(
               "YOUR_SERVICE_ID",
               "YOUR_TEMPLATE_ID",
               {
                   to_email: data.to,
                   to_name: currentUser.name,
                   order_number: data.orderNumber,
                   products: data.products,
                   total: data.total
               }
           );
           console.log('Email sent successfully');
       } catch (error) {
           console.error('Email error:', error);
       }
   }
   ```

### خيار 2: SendGrid

1. سجل في [sendgrid.com](https://sendgrid.com)
2. احصل على API Key
3. استخدم API الخاص بهم:

```javascript
async function sendEmail(data) {
    await fetch('https://api.sendgrid.com/v3/mail/send', {
        method: 'POST',
        headers: {
            'Authorization': 'Bearer YOUR_API_KEY',
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            personalizations: [{
                to: [{ email: data.to }],
                subject: data.subject
            }],
            from: { email: 'noreply@devix.store' },
            content: [{
                type: 'text/html',
                value: data.body
            }]
        })
    });
}
```

### خيار 3: SMTP.js (مباشر من Frontend)

```html
<script src="https://smtpjs.com/v3/smtp.js"></script>
```

```javascript
Email.send({
    Host: "smtp.elasticemail.com",
    Username: "your-email@example.com",
    Password: "your-password",
    To: data.to,
    From: "noreply@devix.store",
    Subject: data.subject,
    Body: data.body
});
```

---

## 🎯 الميزات الرئيسية

### ✅ المميزات المكتملة

- ✨ واجهة احترافية بتصميم عصري
- 🛒 نظام سلة تسوق متقدم
- 👤 نظام تسجيل دخول وإنشاء حساب
- 🎮 ربط حساب Discord إجباري
- 💳 بوابة دفع متعددة (Visa, Mastercard, مدى, Apple Pay)
- 📧 إرسال إيميلات تأكيد تلقائية
- 🔔 إشعارات Discord Webhook فورية
- 📱 تصميم متجاوب لجميع الأجهزة
- 💾 حفظ السلة والبيانات محلياً
- 🔒 حماية متقدمة وتشفير

### 📊 إشعارات Discord المتاحة

1. **تسجيل مستخدم جديد**: معلومات المستخدم + Discord ID
2. **طلب جديد**: تفاصيل الطلب كاملة + معلومات العميل
3. **دفع ناجح**: تأكيد الدفع مع رقم المعاملة
4. **فشل الدفع**: تنبيه مع سبب الفشل
5. **رسالة دعم**: رسائل العملاء مباشرة
6. **تقرير يومي**: ملخص المبيعات اليومية

---

## 💻 استخدام الموقع

### للعملاء:

#### 1. إنشاء حساب
1. اضغط "تسجيل الدخول" من الأعلى
2. اختر "إنشاء حساب"
3. أدخل البيانات (الاسم، الإيميل، رقم الجوال، كلمة المرور)
4. **هام:** اضغط "ربط حساب Discord" (إجباري)
5. اضغط "إنشاء حساب"

#### 2. التسوق
1. تصفح المنتجات
2. اضغط "إضافة للسلة"
3. اذهب للسلة (أيقونة 🛒)
4. راجع المنتجات ويمكنك تعديل الكميات
5. اضغط "إتمام الطلب"

#### 3. الدفع
1. اختر طريقة الدفع
2. أدخل معلومات البطاقة:
   - رقم البطاقة: 4242 4242 4242 4242 (للتجربة)
   - الاسم: أي اسم
   - تاريخ الانتهاء: أي تاريخ مستقبلي
   - CVV: أي 3 أرقام
3. اضغط "دفع آمن"
4. ستحصل على رقم طلب وإيميل تأكيد

### للمطورين:

#### إضافة منتجات جديدة

في `script.js`، عدّل مصفوفة `products`:

```javascript
const products = [
    {
        id: 10, // رقم فريد
        name: 'اسم المنتج',
        price: 999,
        emoji: '🎮', // أيقونة المنتج
        desc: 'وصف المنتج',
        badge: 'جديد' // اختياري
    },
    // ... المنتجات الأخرى
];
```

#### تخصيص الألوان

في `style.css`، عدّل المتغيرات:

```css
:root {
    --primary: #0ea5e9;     /* اللون الأساسي */
    --secondary: #06b6d4;   /* اللون الثانوي */
    --success: #10b981;     /* لون النجاح */
    --danger: #ef4444;      /* لون الخطر */
    /* ... */
}
```

---

## 🔧 الإعدادات المتقدمة

### تغيير رسوم الشحن

في `script.js`:

```javascript
const CONFIG = {
    SHIPPING_COST: 50 // غيّر هذا الرقم
};
```

### تفعيل وضع الإنتاج

1. **حذف console.log**
   - ابحث عن جميع `console.log` واحذفها أو علّق عليها

2. **تفعيل SSL**
   - تأكد من استخدام HTTPS في الاستضافة

3. **تأمين API Keys**
   - لا تضع API keys في Frontend
   - استخدم Backend server لحماية المفاتيح

### إضافة Google Analytics

في `<head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: Discord Webhook لا يعمل

**الحل:**
1. تأكد من صحة رابط الـ webhook
2. تأكد من وجود إنترنت
3. تحقق من Console في المتصفح (F12)
4. جرّب إرسال webhook يدوياً:
   ```javascript
   fetch('WEBHOOK_URL', {
       method: 'POST',
       headers: {'Content-Type': 'application/json'},
       body: JSON.stringify({content: 'test'})
   });
   ```

### المشكلة: البريد الإلكتروني لا يُرسل

**الحل:**
1. تحقق من إعدادات EmailJS
2. تأكد من صحة API keys
3. تحقق من Quota (الحد المسموح)
4. راجع Console للأخطاء

### المشكلة: السلة تُفرغ عند التحديث

**الحل:**
- هذا طبيعي. البيانات محفوظة في localStorage
- لإصلاحه، تأكد من استدعاء `loadCartData()` عند تحميل الصفحة

### المشكلة: الموقع لا يعمل على الموبايل

**الحل:**
1. تأكد من وجود:
   ```html
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   ```
2. اختبر على متصفحات مختلفة
3. استخدم Chrome DevTools للاختبار

---

## 📱 معلومات الاتصال والدعم

- **التحديثات**: تابع المشروع على GitHub
- **الأسئلة**: افتح Issue في GitHub
- **التخصيص**: متاح للطلب

---

## 📄 الترخيص

MIT License - مفتوح المصدر ومجاني للاستخدام الشخصي والتجاري

---

## 🎉 شكر خاص

شكراً لاستخدامك DEVIX Store! 

نتمنى لك تجربة رائعة 🚀

---

## 📚 موارد إضافية

### تعلم المزيد:
- [Discord Webhook Guide](https://discord.com/developers/docs/resources/webhook)
- [Stripe Documentation](https://stripe.com/docs)
- [EmailJS Docs](https://www.emailjs.com/docs/)
- [MDN Web Docs](https://developer.mozilla.org)

### أدوات مفيدة:
- [VS Code](https://code.visualstudio.com/) - محرر الكود الأفضل
- [Postman](https://postman.com/) - لاختبار APIs
- [Figma](https://figma.com/) - للتصميم

---

**نسخة:** 1.0.0  
**آخر تحديث:** ديسمبر 2024  
**المطور:** DEVIX Team